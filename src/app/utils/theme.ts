import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2777AE",
    },
    secondary: {
      main: "#1C2536",
    },
    background: {
      default: "#FEFEFE",
    },
  },
});

export default theme;
